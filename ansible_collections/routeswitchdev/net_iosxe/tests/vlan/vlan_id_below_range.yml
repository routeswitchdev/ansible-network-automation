---
# Scenario: vlan_id is below the supported range.
#
# Expected behavior:
#   - Rejected by tasks/validate.yml's out-of-range check
#     (vlan_id_min = 1).
#   - No device configuration change is made.

vlan_id: -1
vlan_action: create
